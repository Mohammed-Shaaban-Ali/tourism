import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // i want add this domain images.unsplash.com
  images: {
    domains: ["images.unsplash.com"],
  },
};

export default nextConfig;
